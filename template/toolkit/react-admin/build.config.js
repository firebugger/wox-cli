module.exports = {
  entries: '*'                      // `*` 代表会编译所有 `*.entry.js` 入口的文件
  // entries: ['basic-info-edit']   // ['basic-info-edit'] 代表会编译数组内配置的入口文件，例如 `basic-info-edit.entry.js` 入口文件，其他同理
};
